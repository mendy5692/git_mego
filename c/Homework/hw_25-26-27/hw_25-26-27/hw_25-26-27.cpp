#include<stdio.h>

void main(){
	// קליטת מספרים שונים עד אשר נקלטים 3 פעמים הספר 0 והדפסת ממוצע המספרים ללא חישוב ה 0
	/*
	int i = 0, sum = 0, num, zero = 0;
	float final_sum = 0;

	while (zero < 3) {
		printf("Enter a number: ");
		scanf_s("%d", &num);

		if (num == 0) {
			zero++;
		}

		else {
			sum += num;
			i++;
		}

	}

	final_sum = sum / i;
	printf("%f", final_sum);
	*/







}